// Public API barrel for @pro-print/editor.
// Round 1 phase 1: scaffold only — real exports (PrintDesigner component,
// template schema, stores) land in later phases.
import './styles/index.css'

/** Library version, kept in sync with package.json at release time. */
export const version = '0.1.0'
